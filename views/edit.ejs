<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/edit.css">
    <title>Edit Post</title>
</head>
<body>
    <div class="container">
        <h1>Your Post</h1>

        <!-- Form to submit a new blog post -->
        <form action="/user_posts/edit" method="POST">
            <input name="postId" type="hidden" value="<%= post.post_id %>">
            <div class="post_input">
                <label for="title" >title: </label>
                <input type="text" name="title" value="<%= post.title %>" required>

                <label for="description">description: </label>
                <textarea rows="4" name="description" id="description" required><%= post.content %></textarea>
            </div>
                
            <button class="post_button" type="submit">edit</button>

        </form>
    </div>
</body>
</html>